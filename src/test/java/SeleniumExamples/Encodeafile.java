package SeleniumExamples;

import java.io.IOException;
import java.io.OutputStream;
import java.nio.charset.StandardCharsets;

import org.apache.commons.codec.binary.Base64;
import org.apache.commons.io.FileUtils;

import java.io.File;
import java.io.FileInputStream;
import java.io.FileOutputStream;
public class Encodeafile {/*
	
	
	private static String encodeFileToBase64Binary() throws IOException {
	    File file = new File("C:\\Users\\aksaini\\Desktop\\source.gif");
	    byte[] encoded = Base64.encodeBase64(FileUtils.readFileToByteArray(file));
	    return new String(encoded, StandardCharsets.US_ASCII);
	    
	}
	
	private static  void dencodeFileToBase64Binary() throws IOException {
    File file = new File("C:\\Users\\aksaini\\Desktop\\source.gif");
    byte[] encoded = Base64.encodeBase64(FileUtils.readFileToByteArray(file));
	byte[] data = Base64.decodeBase64(encoded);
	try (OutputStream stream = new FileOutputStream("c:/Users/aksaini/Desktop/abc.gif")) {
	    stream.write(data);
	}
	}
	public static void main(String args[]) throws IOException
	{
		String value =encodeFileToBase64Binary();
	    System.out.println(value);
	    dencodeFileToBase64Binary();
	}

*/}
